/**
 * Class which handles tab behavior
 * @param parent
 */
function Tab(controller, parent) {
    this.controller = controller;
    this.parent = parent;
    
    this.render = function() {
        
    };
};