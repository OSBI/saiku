<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <parent>
        <artifactId>saiku</artifactId>
        <groupId>org.saikuanalytics</groupId>
        <version>4.8.0-RC2</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>saiku-webapp</artifactId>
    <version>4.8.0-RC2</version>
    <name>saiku - webapp</name>
    <packaging>war</packaging>
    <build>
        <plugins>
            <!-- The webapp's artifact is a ~100MB war that bundles every
                 transitive dep. Nobody needs to <dependency> it from
                 Maven — consumers either run the launcher fat-jar or
                 build their own webapp. Skip mvn deploy so GH Packages
                 doesn't fill up with multi-hundred-meg blobs per release.
                 The saiku-core/* JARs are the actual consumable surface. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>3.1.4</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <version>6.1.0</version>
                <extensions>true</extensions>

                <configuration>
                    <supportedProjectTypes>
                        <supportedProjectType>war</supportedProjectType>
                    </supportedProjectTypes>
                    <manifestLocation>./src/main/webapp/META-INF</manifestLocation>
                    <instructions>
                        <_wab>src/main/webapp</_wab>
                        <Export-Package>org.saiku</Export-Package>
                        <Bundle-SymbolicName>${project.artifactId}</Bundle-SymbolicName>
                        <Bundle-ClassPath>.,WEB-INF/classes,{maven-dependencies}</Bundle-ClassPath>
                        <Embed-Directory>WEB-INF/lib</Embed-Directory>
                        <Embed-Dependency>*;scope=compile|runtime;</Embed-Dependency>
                        <Embed-Transitive>true</Embed-Transitive>
                        <Web-ContextPath>/saiku</Web-ContextPath>
                        <Webapp-Context />
                    </instructions>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.saikuanalytics</groupId>
                                    <artifactId>saiku-web</artifactId>
                                    <version>${project.version}</version>
                                    <type>jar</type>
                                    <classifier>sources</classifier>
                                    <outputDirectory>target/saiku-webapp-3.0.1-SNAPSHOT/WEB-INF/lib/</outputDirectory>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <!-- First step is to disable the default-war build step. -->
                        <id>default-war</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <!-- Second step is to create an exploded war. Done in prepare-package -->
                        <id>war-exploded</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>exploded</goal>
                        </goals>
                    </execution>
                    <execution>
                        <!-- Last step is to make sure that the war is built in the package phase -->
                        <id>custom-war</id>
                        <phase>package</phase>
                        <goals>
                            <goal>war</goal>
                        </goals>
                        <configuration>
                            <!-- xerces is dragged in transitively (mondrian +
                                 hadoop via hive-jdbc) but its
                                 META-INF/services javax.xml.parsers.* override
                                 hijacks JAXP and Spring 6.2's stricter
                                 namespace validation breaks against it for
                                 applicationContext.xml's
                                 <mvc:annotation-driven/>. JDK 21 has an
                                 internal xerces that respects JAXP's resolver
                                 chain — strip the external copy so the JDK's
                                 implementation wins.

                                 xalan IS bundled (was previously stripped):
                                 JDK 21's built-in XSLTC NPEs when compiling
                                 the xhtml2fo.xsl PDF-export stylesheet
                                 ("Cannot invoke String.hashCode() because
                                 this._stringRep is null" inside XSLTC.compile,
                                 2026-06-07 demo bug). Apache Xalan's
                                 TransformerFactory handles the XSL correctly;
                                 its SPI registration doesn't conflict with
                                 Spring (Spring's complaint was about Xerces's
                                 DocumentBuilderFactory, not Xalan's
                                 TransformerFactory). The matching serializer
                                 jar is kept alongside xalan because xalan
                                 calls into it. -->
                            <!-- netty-3.7.0 (from zookeeper via hive-jdbc) and
                                 netty-all-4.0.23 (from hadoop-hdfs) collide with
                                 the netty-common/buffer 4.1.117 that arrow-memory-netty
                                 18.x needs. With both on the classpath, the resolver
                                 picks netty-all's EmptyByteBuf which throws
                                 UnsupportedOperationException on memoryAddress(),
                                 crashing arrow's NettyAllocationManager.<clinit>
                                 the first time a query executes. -->
                            <packagingExcludes>WEB-INF/lib/saiku-query-${saiku-query.version}.jar,WEB-INF/lib/xercesImpl-*.jar,WEB-INF/lib/netty-3.*.jar,WEB-INF/lib/netty-all-*.jar</packagingExcludes>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <archive>
                        <manifest>
                            <addClasspath>true</addClasspath>
                        </manifest>
                    </archive>
                    <!-- saiku#1036: these two settings used to live in a SECOND
                         maven-war-plugin declaration further down; Maven merged the
                         duplicates with a model warning. Folded here so the
                         effective model is explicit (applies to the exploded and
                         custom-war executions exactly as the merge already did). -->
                    <warSourceExcludes>WEB-INF/saiku-beans.xml</warSourceExcludes>
                    <webResources>
                        <resource>
                            <directory>${project.basedir}/../saiku-ui/dist</directory>
                            <targetPath>ui</targetPath>
                        </resource>
                    </webResources>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>copy-resources</id>
                        <!-- here the phase you need -->
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${basedir}/target/saiku-webapp-${project.version}/WEB-INF</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/webapp/WEB-INF</directory>
                                    <filtering>true</filtering>
                                    <includes>
                                        <include>saiku-beans.xml</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>remove.saiku-query.mondrian</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <!-- Remove mondrian from saiku-query jar
                                     See https://groups.google.com/a/saiku.meteorite.bi/forum/#!topic/user/oER6gEqc46w
                                -->
                                <jar destfile="${basedir}/target/saiku-webapp-${project.version}/WEB-INF/lib/saiku-query-${saiku-query.version}-nomondrian.jar">
                                    <zipfileset src="${basedir}/target/saiku-webapp-${project.version}/WEB-INF/lib/saiku-query-${saiku-query.version}.jar"
                                                excludes="mondrian/"/>
                                </jar>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- saiku#1036: a second maven-antrun-plugin declaration (id=deploy-ui,
                 antrun 1.6, EMPTY deprecated <tasks/>) sat here doing literally
                 nothing since the legacy UI deploy was removed — deleted. Its
                 removal also clears the "Parameter tasks is deprecated" warning. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.19.1</version>
                <executions>
                    <execution>
                        <id>surefire-it</id>
                        <phase>integration-test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <skip>false</skip>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.eclipse.jetty.ee10</groupId>
                <artifactId>jetty-ee10-maven-plugin</artifactId>
                <version>12.0.16</version>
            </plugin>
            <!-- saiku#1036: the duplicate maven-war-plugin declaration that lived
                 here (config-only) is folded into the single declaration above. -->
            <plugin>
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <!-- 1.15.1 fixed Node 22 tarball extraction permission errors;
                     2.0.2 carries forward that fix + drops Maven 3.x support
                     (we're on 3.9+ via the dist build). Goal surface (npm/node
                     install + npm ci) is unchanged. -->
                <version>2.0.2</version>
                <configuration>
                    <workingDirectory>${project.basedir}/../saiku-ui</workingDirectory>
                    <!-- Use saiku-webapp/target/node — local to this module,
                         no '..' traversal across module boundaries. The
                         '..'-based path (saiku-webapp/target/../../saiku-ui/.node)
                         triggered "Could not get write permissions for
                         share/doc/node" on CI Linux runners because the
                         normalisation happens during generate-resources
                         before target/ exists, which leaves the extractor
                         resolving against a not-yet-created directory. -->
                    <installDirectory>${project.build.directory}/node</installDirectory>
                    <nodeVersion>v22.20.0</nodeVersion>
                </configuration>
                <executions>
                    <execution>
                        <id>install-node-and-npm</id>
                        <goals>
                            <goal>install-node-and-npm</goal>
                        </goals>
                        <phase>generate-resources</phase>
                    </execution>
                    <execution>
                        <id>npm-install</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <arguments>ci --no-audit --no-fund</arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm-build</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <arguments>run build</arguments>
                            <environmentVariables>
                                <SAIKU_BASE_PATH>/ui</SAIKU_BASE_PATH>
                            </environmentVariables>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <dependencies>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-taglibs</artifactId>
        </dependency>
        <dependency>
            <groupId>org.saikuanalytics</groupId>
            <artifactId>saiku-service</artifactId>
        </dependency>
        <!-- saiku-sql brings OssieSchemaFactory + the Calcite Ossie adapter
             to the runtime classpath. saiku-service's SaikuOssieConnection
             constructs 'jdbc:calcite:model=...' URLs that reference
             'bi.saiku.ossie.sql.internal.OssieSchemaFactory' by
             fully-qualified name, so the class must be loadable when the
             connection opens. Runtime scope because saiku-service doesn't
             have a compile-time reference (and creating one would be a
             Maven cycle since saiku-sql already depends on saiku-service). -->
        <dependency>
            <groupId>org.saikuanalytics</groupId>
            <artifactId>saiku-sql</artifactId>
            <version>${project.version}</version>
            <scope>runtime</scope>
            <exclusions>
                <!-- avatica-server pulls Jetty 9.4 (its own embedded HTTP surface for
                     the Avatica RPC endpoint). We don't need that inside the launcher —
                     the OSSIE workbench uses Calcite via jdbc:calcite: (in-process); the
                     external Avatica JDBC endpoint is only wired by the standalone
                     'sql-serve' subcommand and packages its own classpath. Including
                     avatica-server here collides with Jetty 12 that the launcher itself
                     depends on: Jetty 9's AbstractLifeCycle wins the shade, and Jetty
                     12's server-startup path then explodes on a missing StopException
                     inner class. -->
                <exclusion>
                    <groupId>org.apache.calcite.avatica</groupId>
                    <artifactId>avatica-server</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!-- saiku#1036: a first hive-jdbc declaration sat here; the one further
             down is a strict superset (adds the JDK-21-critical jdk.tools +
             hbase:* exclusions) and won under Maven's duplicate handling anyway
             — deleted this copy, comments preserved on the survivor. -->
        <dependency>
            <groupId>org.apache.hive</groupId>
            <artifactId>hive-cli</artifactId>
            <exclusions>
                <exclusion><groupId>net.hydromatic</groupId><artifactId>quidem</artifactId></exclusion>
                <exclusion><groupId>net.hydromatic</groupId><artifactId>linq4j</artifactId></exclusion>
                <exclusion><groupId>org.pentaho</groupId><artifactId>pentaho-aggdesigner-algorithm</artifactId></exclusion>
                <exclusion><groupId>org.apache.hive</groupId><artifactId>hive-exec</artifactId></exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.saikuanalytics</groupId>
            <artifactId>saiku-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.saikuanalytics</groupId>
            <artifactId>saiku-olap-util</artifactId>
        </dependency>
        <dependency>
            <groupId>org.saiku</groupId>
            <artifactId>saiku-query</artifactId>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jersey.ext</groupId>
            <artifactId>jersey-spring6</artifactId>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jersey.media</groupId>
            <artifactId>jersey-media-multipart</artifactId>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jersey.media</groupId>
            <artifactId>jersey-media-json-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jersey.inject</groupId>
            <artifactId>jersey-hk2</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jvnet.mimepull</groupId>
            <artifactId>mimepull</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.hsqldb</groupId>
            <artifactId>hsqldb</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>com.gizmodata</groupId>
            <artifactId>quack-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.hive</groupId>
            <artifactId>hive-jdbc</artifactId>
            <exclusions>
                <exclusion><groupId>net.hydromatic</groupId><artifactId>quidem</artifactId></exclusion>
                <exclusion><groupId>net.hydromatic</groupId><artifactId>linq4j</artifactId></exclusion>
                <exclusion><groupId>org.pentaho</groupId><artifactId>pentaho-aggdesigner-algorithm</artifactId></exclusion>
                <!-- hive-exec ships a shaded protobuf 2.x that breaks Calcite
                     Avatica's protobuf 4.x at class-load time
                     (IncompatibleClassChangeError on GeneratedMessageV3
                     vs GeneratedMessage). -->
                <exclusion><groupId>org.apache.hive</groupId><artifactId>hive-exec</artifactId></exclusion>
                <!-- hive-jdbc 2.3 → hive-metastore → hbase-client → hbase-annotations
                     → jdk.tools:1.7 (system-scope ref to JDK 8 tools.jar). Same
                     gone-on-JDK-21 issue as hadoop-common's transitive. -->
                <exclusion><groupId>jdk.tools</groupId><artifactId>jdk.tools</artifactId></exclusion>
                <exclusion><groupId>org.apache.hbase</groupId><artifactId>*</artifactId></exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-common</artifactId>
            <exclusions>
                <exclusion><groupId>com.sun.jersey</groupId><artifactId>*</artifactId></exclusion>
                <exclusion><groupId>com.sun.jersey.contribs</groupId><artifactId>*</artifactId></exclusion>
                <exclusion><groupId>javax.servlet</groupId><artifactId>*</artifactId></exclusion>
                <exclusion><groupId>javax.servlet.jsp</groupId><artifactId>*</artifactId></exclusion>
                <exclusion><groupId>javax.ws.rs</groupId><artifactId>*</artifactId></exclusion>
                <exclusion><groupId>org.mortbay.jetty</groupId><artifactId>*</artifactId></exclusion>
                <exclusion><groupId>tomcat</groupId><artifactId>jasper-compiler</artifactId></exclusion>
                <exclusion><groupId>tomcat</groupId><artifactId>jasper-runtime</artifactId></exclusion>
                <!-- jdk.tools:jdk.tools is a system-scope ref to JDK 8's
                     tools.jar, removed by the JDK 9 module system. On JDK 21
                     it can't be resolved and breaks the build on a fresh
                     Maven cache. Saiku doesn't use any javac-API surface from
                     tools.jar, so excluding is safe. -->
                <exclusion><groupId>jdk.tools</groupId><artifactId>jdk.tools</artifactId></exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
        </dependency>
    </dependencies>
    <reporting>
        <plugins>
            <!-- Produce Source cross references -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jxr-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <aggregate>true</aggregate>
                </configuration>
            </plugin>
            <!-- Unit tests report -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-report-plugin</artifactId>
                <version>2.3</version>
            </plugin>
            <!-- "Release Audit" report (checks license headers etc.) -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>rat-maven-plugin</artifactId>
                <version>1.0-alpha-3</version>
            </plugin>
            <!-- FindBugs Report -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>findbugs-maven-plugin</artifactId>
                <version>3.0.5</version>
                <configuration>
                    <threshold>Normal</threshold>
                    <effort>Default</effort>
                </configuration>
            </plugin>
            <!-- Checkstyle report -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <enableRulesSummary>false</enableRulesSummary>
                </configuration>
            </plugin>
            <!-- Project website generation -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-site-plugin</artifactId>
                <version>4.0.0-M16</version>
                <configuration>
                    <outputEncoding>UTF-8</outputEncoding>
                </configuration>
            </plugin>
        </plugins>
    </reporting>
</project>
